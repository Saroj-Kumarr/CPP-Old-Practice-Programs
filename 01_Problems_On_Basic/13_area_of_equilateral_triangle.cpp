#include <bits/stdc++.h>
using namespace std;
int main()
{
    int side;
    cout<<"enter side of triangle\n";
    cin>>side;

    double area=sqrt(3)/4*side*side;
    cout<<"area is "<<area;
  
return 0;
}